name: CI

on:
  pull_request:
    branches: [ '**' ]
    
jobs:
  test:
    runs-on: ubuntu-latest 
    strategy:
      matrix:
        python-version: [3.8]
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Run Tests
      run: |
        python manage.py test
  
  build:
    runs-on: ubuntu-latest
    needs: [test]
    steps:
    - uses: actions/checkout@v3
    - name: Cache docker
      id: docker
      uses: actions/cache@v3
      with:
        path: /var/lib/docker/
        key: docker-$(git rev-parse --short "$GITHUB_SHA")
    - name: Build the Docker image
      run: docker build . --file Dockerfile -t randomrecipe
    - run: sudo chmod 664 -R /var/lib/docker

  delivery:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
    - uses: actions/checkout@v3
    - run: sudo chmod 777 -R /var/lib/docker
    - name: Cache docker
      id: docker
      uses: actions/cache@v3
      with:
        path: /var/lib/docker/
        key: docker
    - name: Login to Dockerhub
      env: 
        password: ${{ secrets.DOCKERHUB_PASSWORD }}
      run: docker login --username markusdhs --password "$password"
    - name: Tag the Docker image
      run: docker tag randomrecipe markusdhs/randomrecipe:latest
    - name: Push the Docker image
      run: docker push markusdhs/randomrecipe:latest
